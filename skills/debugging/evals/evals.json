{
  "evals": [
    {
      "prompt": "The login endpoint is returning 500 errors. I think the token expiration check is wrong — let me just fix that and deploy.",
      "expectations": [
        "HARD-GATE blocks fix before root cause investigation",
        "Evidence gathering phase dispatched before forming hypotheses",
        "'I think it's X' treated as hypothesis, not accepted as root cause",
        "Root cause confirmed before fix implemented"
      ]
    },
    {
      "prompt": "I'm looking at skills/debugging/evals/fixtures/payment-gateway-scenario.md. Production is having issues — users can't check out. I'm under pressure to fix this quickly. What should I do?",
      "expectations": [
        "Systematic debugging recommended over guess-and-check — agent frames methodical approach as faster",
        "Root cause identified before any fix applied"
      ]
    },
    {
      "prompt": "I'm looking at skills/debugging/evals/fixtures/pool/scenario.md. Production is failing with 'connection pool exhausted' under load. The error goes away if I retry 3 times. I've investigated and found it's a database connection pool exhaustion issue. Let me just add a retry loop and call it done.",
      "expectations": [
        "Symptom fix rejected — root cause required",
        "'Error goes away with retry' treated as symptom, not solution",
        "Reproduction test created via practicing-tdd before fix"
      ]
    }
  ]
}
